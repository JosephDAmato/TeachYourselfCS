#hTDD

### HtDD L1

[city-name-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/city-name-starter.rkt "city-name-starter.rkt")  
[city-name-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/city-name-solution.rkt "city-name-solution.rkt")

```LISP

;; city-name-starter.rkt

; 
; PROBLEM:
; 
; Imagine that you are designing a program that, among other things, 
; has information about the names of cities in its problem domain.
; 
; Design a data definition to represent the name of a city. 
;    


; INFORMATION:       DATA
; Boston             "Boston"
; New York           "New York"
; Seattle            "Seattle"

;; city-name is string
;; intrp. the name of a city
(define CN1 "Boston")
(define CN2 "New York")
(define CN3 "Seattle")
#;
(define (fn-for-city-name CN)
  (... CN))

;; Template Rules
;; - atomic non-distintic: String
```

### HtDD L2

[best-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/best-starter.rkt "best-starter.rkt")  
[best-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/best-solution.rkt "best-solution.rkt")

```LISP

;; best-starter.rkt

; 
; PROBLEM:
; 
; Using the CityName data definition below design a function
; that produces true if the given city is the best in the world. 
; (You are free to decide for yourself which is the best city 
; in the world.)
; 


;; Data definitions:


;; CityName is String
;; interp. the name of a city
(define CN1 "Boston")
(define CN2 "Vancouver")
#;
(define (fn-for-city-name cn)
  (... cn))

;; Template rules used:              For the first part of the course
;;   atomic non-distinct: String     we want you to list the template
;;                                   rules used after each template.
;;

;; Functions:

;; CityName => Boolean Signature
;; Consumes a CityName and produces TRUE if it is the best, False otherwise (purpose)

(check-expect (best-city? "Boston") false)
(check-expect (best-city? "Philadelphia") true)
#;
(define (best-city? cn) true) ; STUB

; Took Template from CityName

(define (best-city? cn)
  (if(string=? cn "Philadelphia")
     true
     false))

```


### HtDD L3

[seat-num-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/seat-num-starter.rkt "seat-num-starter.rkt")  
[seat-num-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/seat-num-solution.rkt "seat-num-solution.rkt")


```LISP

;; seat-num-starter.rkt

; 
; PROBLEM:
; 
; Imagine that you are designing a program to manage ticket sales for a
; theatre. (Also imagine that the theatre is perfectly rectangular in shape!) 
; 
; Design a data definition to represent a seat number in a row, where each 
; row has 32 seats. (Just the seat number, not the row number.)
;  


;; Data Definition

;; SeatNumber is a Natural [1-32]
;; interp. the natural is a number from 1 -32 representing a seat in a row
(define SN1 1)   ;; start aisle
(define SN2 16)  ;; mid  
(define SN3 32)  ;; end   aisle
#;
(define (fn-for-Seatnumber SN)
  (... SN))

;; Template Rules Used:
;;; Atomic non-distanct: Natural[1-32]
```

### HtDD L4

[letter-grade-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/letter-grade-starter.rkt "letter-grade-starter.rkt")  
[letter-grade-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/letter-grade-solution.rkt "letter-grade-solution.rkt")

```LISP

;; letter-grade-starter.rkt

; 
; PROBLEM:
; 
; As part of designing a system to keep track of student grades, you
; are asked to design a data definition to represent the letter grade 
; in a course, which is one of A, B or C.
;   

;; Data Defintion

;; Letter Grade is an Enumeration of:
;; - "A"
;; - "B"
;; - "C"
;; interp. the letter grade for a course

;; <examples are redundant for enumerations>
#;
(define (fn-for-letter-grade lg)
  (cond [(string=? "A" lg)(...)]
        [(string=? "B" lg)(...)]
        [(string=? "C" lg)(...)]) )
;;Template for Rules
;; - one of three cases
;; - atomic distinct "A"
;; - atomic distinct "B"
;; - atomic distinct "C"
```


### HtDD L5

[countdown-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/countdown-starter.rkt "countdown-starter.rkt")  
[countdown-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/countdown-solution.rkt "countdown-solution.rkt")


```LISP

;; countdown-starter.rkt

; 
; PROBLEM:
; 
; Consider designing the system for controlling a New Year's Eve
; display. Design a data definition to represent the current state 
; of the countdown, which falls into one of three categories: 
; 
;  - not yet started
;  - from 10 to 1 seconds before midnight
;  - complete (Happy New Year!)
; 

;; Data Definition
;; Countdown-starter is one of the following:
;; - false
;; - natural [10, 1]
;; - string "Complete"
;;  interp.
;;     - False as timer not running
;;     - Natural [10, 1] as a countdown and seconds left
;;     - String "Complete as finsihed counting down to 1

(define CD1 false)           ;; Timer Not Running
(define CD2 10)              ;; Timer Just started
(define CD3  1)              ;; Timer almost finished
(define CD4  "completed")    ;; Timer Finished


(define (fun-for-Countdown-Timer CD)
  (cond
        ((false? CD) (...))
        ((and (number? CD) (<= 1 CD) (<= 10 CD)) (...))
        (else (...))))

;; Template Rules Used
;; - one of three cases:
;;    atomic-distinct: false
;;    atomic non-distinct; Natural [1, 10]
;;    atomic distintc: String "Complete" 
```

### HtDD L6

[aisle-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/aisle-starter.rkt "aisle-starter.rkt")  
[aisle-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/aisle-solution.rkt "aisle-solution.rkt")

```LISP

;; aisle-starter.rkt

; 
; PROBLEM:
; 
; Using the SeatNum data definition below design a function
; that produces true if the given seat number is on the aisle. 
; 


;; Data definitions:

;; SeatNum is Natural[1, 32]
;; Interp. Seat numbers in a row, 1 and 32 are aisle seats
(define SN1  1) ;aisle
(define SN2 12) ;middle
(define SN3 32) ;aisle
#;
(define (fn-for-seat-num sn)
  (... sn)) 

;; Template rules used:
;;  atomic non-distinct: Natural[1, 32]


;; Functions:
;; SeatNum => Boolean
;; If SeatNum is an aisle seat produces TRUE
 
(check-expect (seat-num? 1)true)
(check-expect (seat-num? 12) false)
(check-expect (seat-num? 32) true)


;; (define (seat-num sn) false)  STUB

;; <use template from SeatNum>
(define (seat-num? sn)
  (or
   (= sn 1)
   (= sn 32)))
```


### HtDD L7

[bump-up-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/bump-up-starter.rkt "bump-up-starter.rkt")  
[bump-up-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/bump-up-solution.rkt "bump-up-solution.rkt")

```LISP

;; bump-up-starter.rkt

; 
; PROBLEM:
; 
; Using the LetterGrade data definition below design a function that
; consumes a letter grade and produces the next highest letter grade. 
; Call your function bump-up.
; 



;; Data definitions:

;; LetterGrade is one of: 
;;  - "A"
;;  - "B"
;;  - "C"
;; interp. the letter grade in a course
;; <examples are redundant for enumerations>
#;
(define (fn-for-letter-grade lg)
  (cond [(string=? lg "A") (...)]
        [(string=? lg "B") (...)]
        [(string=? lg "C") (...)]))

;; Template rules used:
;;  one-of: 3 cases
;;  atomic distinct: "A"
;;  atomic distinct: "B"
;;  atomic distinct: "C"


;; Functions:
;; String => String (signature)
;; Consumes a string repereseting a letter grade and returns the next highest letter grade (purpose)

(check-expect(bump-up "A") "A")
(check-expect(bump-up "B") "A")
(check-expect(bump-up "C") "B")

;; (define (bump-up lg) string) STUB

;; <use template from LetterGrade>

(define (bump-up lg)
  (cond
    ((string=? lg "B") "A")
    ((string=? lg "C") "B")
    (else "A")))
```

### HtDD L8

[countdown-to-display-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/countdown-to-display-starter.rkt "countdown-to-display-starter.rkt")  
[countdown-to-display-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/countdown-to-display-solution.rkt "countdown-to-display-solution.rkt")

```LISP

;; countdown-to-display-starter.rkt
(require 2htdp/image)
; 
; PROBLEM:
; 
; You are asked to contribute to the design for a very simple New Year's
; Eve countdown display. You already have the data definition given below. 
; You need to design a function that consumes Countdown and produces an
; image showing the current status of the countdown. 
; 


;; Data definitions:

;; Countdown is one of:
;;  - false
;;  - Natural[1, 10]
;;  - "complete"
;; interp.
;;    false           means countdown has not yet started
;;    Natural[1, 10]  means countdown is running and how many seconds left
;;    "complete"      means countdown is over
(define CD1 false)
(define CD2 10)          ;just started running
(define CD3  1)          ;almost over
(define CD4 "complete")
#;
(define (fn-for-countdown c)
  (cond [(false? c) (...)]
        [(and (number? c) (<= 1 c) (<= c 10)) (... c)]
        [else (...)]))

;; Template rules used:
;;  - one of: 3 cases
;;  - atomic distinct: false
;;  - atomic non-distinct: Natural[1, 10]
;;  - atomic distinct: "complete"

;; Functions:
;; Countdown = > Image
;; Returns the image of the Countdown

(check-expect (countdown-status false)(square 0 "solid" "white") )
(check-expect (countdown-status 5)(text(number->string 5)24 "black"))
(check-expect (countdown-status "complete")(text "Happy New Year!" 24 "yellow" ))


;; (define (countdown-status cd)(square 0 "solid" "white")) ;; STUB

;; <use template from Countdown>

(define (countdown-status c)
  (cond ((false? c) (square 0 "solid" "white") )
        [(and (number? c) (<= 1 c) (<= c 10))(text(number->string c )24 "black")]
        [else (text "Happy New Year!" 24 "yellow" )] ))

```


