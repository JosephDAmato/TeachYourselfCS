#Compound

### Self-Ref L1
; Imagine that you are designing a program that will keep track of
; your favorite Quidditch teams. (http://iqasport.org/).
[prob location](https://github.com/ossu/spd-starters/tree/main/Week-4/4a/03_list_data_definition)
```scheme


;; quidditch-starter.rkt

; 
; PROBLEM:
; 
; Imagine that you are designing a program that will keep track of
; your favorite Quidditch teams. (http://iqasport.org/).
; 
; Design a data definition to represent a list of Quidditch teams. 
;    


; ListOfString is one of:
;   - empty
;   - (cons String ListofString)
;intrep a list of strings
(define LOS1 empty)
(define LOS2 (cons "McGill" empty))
(define LOS3 (cons "UBC" (cons "McGill" empty)))

(define (fn-for-los los)
  (cond [(empty? los)(...)] ; atomic distinct
       [else
        (... (first lost)  ; compound data
             (fn-for-los (rest los)))]))
;;Tempalte Rules Used
;;One of: 2 cases
;; - Atomic
;; - Compond: (cons String ListofString)

; PROBLEM:
; 
; We want to know whether your list of favorite Quidditch teams includes
; UBC! Design a function that consumes ListOfString and produces true if 
; the list includes "UBC".
; 

;; ListofString -> Boolean
;; Produce true if los includes "UBC"

(check-expect (contains-ubc? empty) false)
(check-expect (contains-ubc? (cons "McGilll" empty)) false)
(check-expect (contains-ubc? (cons "UBC" (cons "McGill" empty))) true)
(check-expect (contains-ubc? (cons "McGill"(cons "UBC" empty))) true)
;(define (contains-ubc? los) false) ;STUB

; Template from ListOfStrings
(define (contains-ubc? los)
  (cond [(empty? los) false] ; atomic distinct
       [else
        (if (string=? (first los) "UBC")
            true
             (contains-ubc? (rest los)))]))
```


## L2
 You've been asked to design a program having to do with all the owls in the owlery.

```scheme

;; designing-with-lists-1-starter.rkt

; 
; PROBLEM:
; 
; You've been asked to design a program having to do with all the owls
; in the owlery.
; 
; (A) Design a data definition to represent the weights of all the owls. 
;     For this problem call it ListOfNumber.
; (B) Design a function that consumes the weights of owls and produces
;     the total weight of all the owls.
; (C) Design a function that consumes the weights of owls and produces
;     the total number of owls.
;     

;; Data Definitions

;; ListofNumber is one of:
;;  - empty
;;  - (cons Natural ListOfNumber)
;;  - intrep. each number as the weight of an owl ounces

(define LON1 empty)
(define LON2 (cons 10 empty))
(define LON3 (cons 34 (cons 10 empty)))

#;
(define (fn-for-lon lon)
  (cond [(empty? lon)(...)]
        [else
         (... (cr lon)       
              (fn-forlon (cdr lon)))]))

;;Template Rules Used:
;;  - One of Two Cases
;;  - Atomic NonDistinct
;;  - Compound: (cons Number ListOfNumber)




;; Functions:
;; ListOfNumber -> Number
;; Returns the total of all numbers in the list
(check-expect (weight-of-owls LON1) 0)
(check-expect (weight-of-owls LON2) 10)
(check-expect (weight-of-owls LON3) 44)


;;(define (weight-of-owls lon) 0);;STUB
;;Template from LON above:
(define (weight-of-owls lon)
  (cond [(empty? lon) 0 ]
        [else
         (+ (car lon)       
              (weight-of-owls (cdr lon)))]))

;; ListOfNumbers -> Natural
;; Countst the total items in the list
;;
(check-expect (number-of-owls LON1) 0)
(check-expect (number-of-owls LON2) 1)
(check-expect (number-of-owls LON3) 2)

;;(define (number-of-owls lon) 0);; STUB

;;Template from LON above:
(define (number-of-owls lon)
  (cond [(empty? lon) 0 ]
        [else
         (+ 1       
              (number-of-owls (cdr lon)))]))

```
### Self-Ref P1
Design a function to calculate the total number of individual characters in a list of strings
[total-string-length-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/total-string-length-starter.rkt "total-string-length-starter.rkt")  
[total-string-length-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/total-string-length-solution.rkt "total-string-length-solution.rkt")

```LISP


;; total-string-length-starter.rkt

;; =================
;; Data definitions:

; 
; Remember the data definition for a list of strings we designed in Lecture 5c:
; (if this data definition does not look familiar, please review the lecture)
; 


;; ListOfString is one of: 
;;  - empty
;;  - (cons String ListOfString)
;; interp. a list of strings

(define LS0 empty) 
(define LS1 (cons "a" empty))
(define LS2 (cons "a" (cons "b" empty)))
(define LS3 (cons "c" (cons "b" (cons "a" empty))))

#;
(define (fn-for-los los) 
  (cond [(empty? los) (...)]
        [else
         (... (first los)
              (fn-for-los (rest los)))]))

;; Template rules used: 
;; - one of: 2 cases
;; - atomic distinct: empty
;; - compound: (cons String ListOfString)
;; - atomic non-distinct: (first los) is  String
;; - self-reference: (rest los) is ListOfString

;; =================
;; Functions:

; 
; PROBLEM:
; 
; Design a function that consumes a list of strings and determines the total 
; number of characters (single letters) in the list. Call it total-string-length 
; 

;; ListOfString -> Number
;; Returns the total number of chars (singles letters) in a list
;;

(check-expect(total-string-length LS0) 0)
(check-expect(total-string-length LS1) 1)
(check-expect(total-string-length LS2) 2)
(check-expect(total-string-length LS3) 3)

;; (define (total-string-length los) 10) ; STUB

;; Template from LOS above:

(define (total-string-length los) 
  (cond [(empty? los) 0]
        [else
         (+ (string-length (first los))
              (total-string-length (rest los)))]))

```

### Self-Ref P2

Design a function to double every number in a list
[double-all-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/double-all-starter.rkt "double-all-starter.rkt")  
[double-all-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/double-all-solution.rkt "double-all-solution.rkt")

```LISP


;; double-starter.rkt

;; =================
;; Data definitions:

; 
; Remember the data definition for a list of numbers we designed in Lecture 5f:
; (if this data definition does not look familiar, please review the lecture)
; 


;; ListOfNumber is one of:
;;  - empty
;;  - (cons Number ListOfNumber)
;; interp. a list of numbers
(define LON1 empty)
(define LON2 (cons 60 (cons 42 empty)))
#;
(define (fn-for-lon lon)
  (cond [(empty? lon) (...)]
        [else
         (... (first lon)
              (fn-for-lon (rest lon)))]))

;; Template rules used:
;;  - one of: 2 cases
;;  - atomic distinct: empty
;;  - compound: (cons Number ListOfNumber)
;;  - self-reference: (rest lon) is ListOfNumber

;; =================
;; Functions:

; 
; PROBLEM:
; 
; Design a function that consumes a list of numbers and doubles every number 
; in the list. Call it double-all.
; 


;; ListOfNumbers -> ListOfNumber
;; Outputs a ListOfNumbers where each number has been Doubled

(check-expect (double-all LON1) empty)
(check-expect (double-all LON2) (cons 120(cons 84 empty)))
;;(define (double-all lon) 10) ;STUB

;;Template From LON Above:

(define (double-all lon)
  (cond [(empty? lon) empty]
        [else
         (cons (* 2 (car lon))
              (double-all (cdr lon)))]))
```

### Self-Ref P3
Design a data definition to represent a list of booleans, and a function to determine if all values in a given list are true.

[boolean-list-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/boolean-list-starter.rkt "boolean-list-starter.rkt")  
[boolean-list-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/boolean-list-solution.rkt "boolean-list-solution.rkt")


```LISP


;; boolean-list-starter.rkt

;; =================
;; Data definitions:

; 
; PROBLEM A:
; 
; Design a data definition to represent a list of booleans. Call it ListOfBoolean. 
; 

;; ListOfBoolean is one of:
;;  - empty
;;  - (cons Boolean ListOfBoolean)

;; Interp. as a list of Booleans
(define LOB1 empty)
(define LOB2 (cons true empty))
(define LOB3 (cons true (cons false empty)))

#;
(define (fn-for-lob lob)
  (cond [(empty? lob) (...)]
        [else
            (... (car lob)
            (fn-for-lob(cdr lob)))]))
;;Data Template Used:
;;  - One of Two cases
;;  - Atomic Distinct
;;  - Compound (cons boolean ListOfBoolean)
;;  - Self-Ref (cdr lob) is ListOfBoolean

;; =================
;; Functions:

; 
; PROBLEM B:
; 
; Design a function that consumes a list of boolean values and produces true 
; if every value in the list is true. If the list is empty, your function 
; should also produce true. Call it all-true?
; 


;; ListOfBoolean -> Boolean
;; Returns True if list if all true or  empty

(check-expect (all-true LOB1) true)
(check-expect (all-true LOB2) true)
(check-expect (all-true LOB3) false)

;; (define (all-true lob ) false) ;stub

;; Template from LOB above:

(define (all-true lob)
  (cond [(empty? lob) true]
        [else
            (and
                (car lob)
                (all-true(cdr lob)))]))
        





```

### Self-Ref P4
Design a function to add '!' to each string in a list of strings.

[yell-all-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/yell-all-starter.rkt "yell-all-starter.rkt")  
[yell-all-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/yell-all-solution.rkt "yell-all-solution.rkt")

```LISP

;; yell-all-starter.rkt

;; =================
;; Data definitions:

; 
; Remember the data definition for a list of strings we designed in Lecture 5c:
; (if this data definition does not look familiar, please review the lecture)
; 


;; ListOfString is one of: 
;;  - empty
;;  - (cons String ListOfString)
;; interp. a list of strings

(define LS0 empty) 
(define LS1 (cons "a" empty))
(define LS2 (cons "a" (cons "b" empty)))
(define LS3 (cons "c" (cons "b" (cons "a" empty))))

#;
(define (fn-for-los los) 
  (cond [(empty? los) (...)]
        [else
         (... (first los)
              (fn-for-los (rest los)))]))

;; Template rules used: 
;; - one of: 2 cases
;; - atomic distinct: empty
;; - compound: (cons String ListOfString)
;; - self-reference: (rest los) is ListOfString

;; =================
;; Functions:

; 
; PROBLEM:
; 
; Design a function that consumes a list of strings and "yells" each word by 
; adding "!" to the end of each string.
; 

;; ListOfString -> ListOfString
;; Addes a "!" to each element in the string to mimic yelling

(check-expect (yells LS0) empty)
(check-expect (yells LS1)(cons "a!" empty))
(check-expect (yells LS2)(cons "a!"(cons "b!" empty)))
(check-expect (yells LS3)(cons "c!"(cons "b!" (cons "a!" empty)))) 

;(define (yells los) " ") ; STUB

;; Template from LOS above:

(define (yells los) 
  (cond [(empty? los) empty]
        [else
         (cons (string-append (first los) "!")
              (yells (rest los)))]))

```

### Self-Ref P5
Design a function to find the largest number in a list of numbers.

[largest-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/largest-starter.rkt "largest-starter.rkt")  
[largest-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/largest-solution.rkt "largest-solution.rkt")
```LISP


;; largest-starter.rkt

;; =================
;; Data definitions:

; 
; Remember the data definition for a list of numbers we designed in Lecture 5f:
; (if this data definition does not look familiar, please review the lecture)
; 


;; ListOfNumber is one of:
;;  - empty
;;  - (cons Number ListOfNumber)
;; interp. a list of numbers
(define LON1 empty)
(define LON2 (cons 60 (cons 42 empty)))
(define LON3 (cons 10( cons 42069 empty)))
#;
(define (fn-for-lon lon)
  (cond [(empty? lon) (...)]
        [else
         (... (first lon)
              (fn-for-lon (rest lon)))]))

;; Template rules used:
;;  - one of: 2 cases
;;  - atomic distinct: empty
;;  - compound: (cons Number ListOfNumber)
;;  - self-reference: (rest lon) is ListOfNumber

;; =================
;; Functions:

; 
; PROBLEM:
; 
; Design a function that consumes a list of numbers and produces the largest number 
; in the list. You may assume that all numbers in the list are greater than 0. If
; the list is empty, produce 0.
; 

;; ListOfNumber - > Number
;; Produces the Largest number from the list
;;
(check-expect (largest LON1) 0)
(check-expect (largest LON2) 60)
(check-expect (largest LON3) 42069)

;; (define (largest lon) 10) ; STUB
(first (rest LON3))
;; Template from LON above:

(define (largest lon)
  (cond [(empty? lon) 0]   ;BASE
        [else
         (if (> (first lon)(largest (rest lon)))
             (first lon)
             (largest (rest lon)))]))
```

### Self-Ref P6
Design a data definition to represent a list of images, and a function to find the sum of areas from a list of images.

[image-list-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/image-list-starter.rkt "image-list-starter.rkt")  
[image-list-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/image-list-solution.rkt "image-list-solution.rkt")
```LISP
(require 2htdp/image)

;; image-list-starter.rkt

;; =================
;; Data definitions:

; 
; PROBLEM A:
; 
; Design a data definition to represent a list of images. Call it ListOfImage. 
; 

;; ListOfImage is:
;;  - empty
;;  - (cons Image ListOfImage)
;; Intrep as a list of 2HtDP Imamges
(define LOI1 empty)
(define LOI2 (cons (rectangle 10 20 "solid" "green") empty))
(define LOI3 (cons (rectangle 21 43 "outline" "red")(cons (rectangle 10 20 "solid" "green") empty)))

(define (fn-for-loi loi)
  (cond [(empty? loi) (...)]
        [else
         (... (car loi)
         (fn-for-loi (cdr loi)))]
        ))

;; one of: 2 cases
;;   - atomic distinct: empty
;;   - compound: (cons Image ListOfImage)
;;   - self-reference: (cdr loi) is ListOfImage

;; =================
;; Functions:

; 
; PROBLEM B:
; 
; Design a function that consumes a list of images and produces a number 
; that is the sum of the areas of each image. For area, just use the image's 
; width times its height.
; 

;; ListOfImage -> Number
;; Produces the total area of all images in the list
;; 
(check-expect (area LOI1) 0)
(check-expect (area LOI2) (* 10 20))
(check-expect (area LOI3) (+ (* 21 43)(* 10 20)))

;; (define (area loi) 10) ; STUB

;; Template from LOI above

(define (area loi)
  (cond [(empty? loi) 0]
        [else
         (+ (* (image-height (car loi)) (image-width (car loi)))
         (area (cdr loi)))]
        ))


```

