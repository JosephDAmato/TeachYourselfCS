	#Compound

### Compound  L3
A cow that wanders back and forth across the screen.

|                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ![](https://edge.edx.org/c4x/UBCx/CPSC1101/asset/blue_square.png "medium") | [cowabunga-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-starter.rkt "cowabunga-starter.rkt")  <br>[cowabunga-v0.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v0.rkt "cowabunga-v0.rkt")  <br>[cowabunga-v1.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v1.rkt "cowabunga-v1.rkt")  <br>[cowabunga-v2.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v2.rkt "cowabunga-v2.rkt")  <br>[cowabunga-v3.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v3.rkt "cowabunga-v3.rkt")  <br>[cowabunga-v4.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v4.rkt "cowabunga-v4.rkt")  <br>[cowabunga-v5.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v5.rkt "cowabunga-v5.rkt")  <br>[cowabunga-v6.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/cowabunga-v6.rkt "cowabunga-v6.rkt") |

```LISP

; 
; PROBLEM:
; 
; As we learned in the cat world programs, cats have a mind of their own. When they 
; reach the edge they just keep walking out of the window.
; 
; Cows on the other hand are docile creatures. They stay inside the fence, walking
; back and forth nicely.
; 
; Design a world program with the following behaviour:
;    - A cow walks back and forth across the screen.
;    - When it gets to an edge it changes direction and goes back the other way
;    - When you start the program it should be possible to control how fast a
;      walker your cow is.
;    - Pressing space makes it change direction right away.
;    
; To help you here are two pictures of the right and left sides of a lovely cow that 
; was raised for us at Brown University.
; 
; .     .
; 
; Once your program works here is something you can try for fun. If you rotate the
; images of the cow slightly, and you vary the image you use as the cow moves, you
; can make it appear as if the cow is waddling as it walks across the screen.
; 
; Also, to make it look better, arrange for the cow to change direction when its
; nose hits the edge of the window, not the center of its body.
; 


(require 2htdp/image)
(require 2htdp/universe)

;; Cowabunga is a cow walking about the screen

;; =================
;; Constants:
(define HEIGHT 500)
(define WIDTH 600)
(define CTR-Y (/ HEIGHT 2))
(define MTS (empty-scene WIDTH HEIGHT))
(define LCOW .)
(define RCOW .)
;; =================
;; Data definitions:
(define-struct cow (x dx))
;; Cow is (make-cow( Natural[0, WIDTH], Integer))
;; Interp (make-cow x dx) is a cow with coord x and x velocity of dx
;;        x is center of cow
;;        x is in creen coord (pixels)
;;        dx is in pixels per tick

(define C1 (make-cow 100 5))          ;; at pos 10 moving left to right
(define C2 (make-cow (/ WIDTH 2) -5)) ;; Middle of screen moving right to left

;; Template Rules Used
;; - Compound Data: 2 FIELDS
#;
(define (fn-for-cow c)
  (... (cow-x c)        ;; Natural [0, WIDTH]
       (cow-dx c)))     ;; Integer

;; =================
;; Functions:

;; Cow -> Cow
;; start the world with (main 0 5)
;; 
(define (main c)
  (big-bang c                        ; Cow
            (on-tick   next-cow)     ; Cow -> Cow
            (to-draw   render-cow)   ; Cow -> Image
            (on-key    handle-key))) ; Cow KeyEvent -> Cow

;; Cow -> Cow
;; produce the next x position of cow
;;
(check-expect(next-cow (make-cow 20  3))(make-cow (+ 20 3)  3))      ; moving left to right -> 
(check-expect(next-cow (make-cow 20 -3))(make-cow (- 20 3) -3))      ; moving right to left <-  

(check-expect(next-cow (make-cow (- WIDTH 3) 3)) (make-cow WIDTH 3)) ; edge cases
(check-expect(next-cow (make-cow 3 -3))(make-cow 0 -3))               ; edge cases

(check-expect(next-cow (make-cow (- WIDTH 2) 3)) (make-cow WIDTH -3)); moving past edge
(check-expect(next-cow (make-cow 2 -3)) (make-cow 0 3))              ; moving past edge
;; (define (next-cow c) (... c)) ;stub

;; Template from Cow Above:

(define (next-cow c)
  (cond[(> (+  (cow-x c)(cow-dx c)) WIDTH)(make-cow WIDTH  (- (cow-dx c)))]
       [(< (+  (cow-x c)(cow-dx c))     0)(make-cow     0  (- (cow-dx c)))]
       [else
        (make-cow (+ (cow-x c)(cow-dx c))
              (cow-dx c))]))


;; Cow -> Image
;; render an image of a cow at the x,y coord and in its moving direction  
(check-expect (render-cow (make-cow 99 3))
              (place-image RCOW 99 CTR-Y MTS))
(check-expect (render-cow(make-cow 40 -3))
              (place-image LCOW 40 CTR-Y MTS))
       
; (define (render-cow c) MTS) ;stub

(define (render-cow c)
  (place-image (choose-image c) (cow-x c) CTR-Y MTS)) ; choose-image is a helper. Splits functionality so render-image is doing two things.
;; Cow -. Image
;; produce the correct RCOW or LCOW for direction of dx
(check-expect(choose-image (make-cow 10 3)) RCOW)
(check-expect(choose-image (make-cow 45 -3)) LCOW)
(check-expect(choose-image (make-cow 0 -3)) LCOW)
;; (define (choose-image c) RCOW) ;; Stub

(define (choose-image c)
  (if (> (cow-dx c) 0)
      RCOW
      LCOW))

;; Cow KeyEvent -> Cow
;; reverse direction of cow-dx when spacebar is pressed

(check-expect(handle-key (make-cow 100 -5) " ") (make-cow 100 5))
(check-expect(handle-key (make-cow 100 5) " ")(make-cow 100 -5))
(check-expect(handle-key (make-cow 44 -9) "a")(make-cow 44 -9))

;; (define(handle-key c ke) c) ; stub

;; Template from Cow Above

(define (handle-key c ke)
  (if (key=? ke " ")(make-cow (cow-x c)(-(cow-dx c)))
   (make-cow (cow-x c)(cow-dx c))))

```

### Compound P1
Design a compound data definition to represent movies, and a function to compare their release dates.

[movie-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/movie-starter.rkt "movie-starter.rkt")  
[movie-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/movie-solution.rkt "movie-solution.rkt")

```LISP

;; movie-starter.rkt

;; =================
;; Data definitions:

; 
; PROBLEM A:
; 
; Design a data definition to represent a movie, including  
; title, budget, and year released.
; 
; To help you to create some examples, find some interesting movie facts below: 
; "Titanic" - budget: 200000000 released: 1997
; "Avatar" - budget: 237000000 released: 2009
; "The Avengers" - budget: 220000000 released: 2012
; 
; However, feel free to resarch more on your own!
; 

(define-struct movie (title budget year))
;; Movie is (make-movie (String Natural Natural))
;; Interp. (make-movie (title budget year)) as a movie w/
;;                     - title being the name of the movie
;;                     - budget being a number representing the cost
;;                     - year being a number representing a year it was released

(define M1 (make-movie "Titantic" 200000000 1997))
(define M2 (make-movie "Avatar"  237000000 2009))
(define M3 (make-movie "The Avengers" 220000000 2012))
#;
(define (fn-fr-movie m)  
  (...(movie-title m)    ; String
      (movie-budget m)   ; Number
      (movie-year m)))   ; Number

;; Template Rules Used
;; Compound data: 3 Fields

;; =================
;; Functions:

; 
; PROBLEM B:
; 
; You have a list of movies you want to watch, but you like to watch your 
; rentals in chronological order. Design a function that consumes two movies 
; and produces the title of the most recently released movie.
; 
; Note that the rule for templating a function that consumes two compound data 
; parameters is for the template to include all the selectors for both 
; parameters.
; 

;; Movie, Movie -> String
;; Produces the movie Title with the earliest year

(check-expect (movie-order M1 M2) "Avatar")
(check-expect (movie-order M3 M2) "The Avengers")



;; Wrong Way.... Corrected Below :(define(movie-order m)... m) ;STUB
;; (define (movie-order m1 m2) "")

;; use template from above

#;
(define (fn-for-movie m1 m2)
  (... (movie-title m1)
       (movie-budget m1)
       (movie-year m1)
       (movie-title m2)
       (movie-budget m2)
       (movie-year m2)))


(define (movie-order m1 m2)  
  (if (< (movie-year m1) (movie-year m2))
      (movie-title m2)    
      (movie-title m1))) 

```


### Compound P3
Design a compound data definition to represent students, and a function to monitor their allergies


[student-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/student-starter.rkt "student-starter.rkt")  
[student-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/student-solution.rkt "student-solution.rkt")

```LISP

;; student-starter.rkt

;; =================
;; Data definitions:

; 
; PROBLEM A:
; 
; Design a data definition to help a teacher organize their next field trip. 
; On the trip, lunch must be provided for all students. For each student, track 
; their name, their grade (from 1 to 12), and whether or not they have allergies.
; 

(define-struct student (name grade allergies))
;; make-student( String Natural[1,12] Boolean )
;; Interp. student to be a students name, their grade level and if they have allergies

(define S1 (make-student "Sammy" 6 true))
(define S2 (make-student "Debra" 2 false))
(define S3 (make-student "Axel" 10 true))
#;
(define (fn-for-student SL)      
  (...(student-name SL)          ; String
       (student-grade SL)        ; Natural [1,10]
       (student-allergies SL)))  ; Boolean

;; Templates Rules Used:
;;  - Compound data: 3 Fields

;; =================
;; Functions:

; 
; PROBLEM B:
; 
; To plan for the field trip, if students are in grade 6 or below, the teacher 
; is responsible for keeping track of their allergies. If a student has allergies, 
; and is in a qualifying grade, their name should be added to a special list. 
; Design a function to produce true if a student name should be added to this list.
; 

;; student-grade -> Boolean
;; Return true if a student is grade [1, 6] and has allergies, false if they are either grade [7,10] or don't have allergies

(check-expect (hasAllergies? S1) true)
(check-expect(hasAllergies? S2) false)
(check-expect (hasAllergies? S3) false)

;(define (hasAllergies? s) ...) ;STUB

;; Use Template from make-Student

(define (hasAllergies? s)      
  (if (and (student-allergies s)(<= (student-grade s) 6))
      true
      false))
        
```

### Compound P4
Design a compound data definition to represent trips, and a function to compare their lengths.

[growing-grass-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/growing-grass-starter.rkt "growing-grass-starter.rkt")  
[growing-grass-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/growing-grass-solution.rkt "growing-grass-solution.rkt")

```LISP
(require 2htdp/image)
(require 2htdp/universe)

;; growing-grass-starter.rkt

; 
; PROBLEM:
; 
; Design a world program as follows:
; 
; The world starts off with a piece of grass waiting to grow. As time passes, 
; the grass grows upwards. Pressing any key cuts the current strand of 
; grass to 0, allowing a new piece to grow to the right of it.
; 
; Starting display:
; 
; .
; 
; After a few seconds:
; 
; .
; 
; After a few more seconds:
; 
; .
; 
; Immediately after pressing any key:
; 
; .
; 
; A few more seconds after pressing any key:
; 
; .
; 
; NOTE 1: Remember to follow the HtDW recipe! Be sure to do a proper domain 
; analysis before starting to work on the code file.
; 




(require 2htdp/image)
(require 2htdp/universe)

;; My grass growing program

;; =================
;; Constants:
(define WIDTH 900)
(define HEIGHT 500)
(define MTS (rectangle WIDTH HEIGHT "solid" "lightblue"))
(define GRASS-WIDTH 5)
(define NEXT-POS-X 10)
(define GROWTH-RATE (+ 1 (random 2)))
(define CTR-Y HEIGHT)


;; =================
;; Data definitions:
(define-struct Grass (x h))
;; Grass is (make-Grass Number (0, WIDTH], Integer )
;; Interp as the state of a piece of grass growing.
;;          - x is the postion on scence along thr X-Axis
;;          - h is the height of a piece of grass

(define G1 (make-Grass 10 45))
(define G2 (make-Grass 40 99))

(define (fn-for-Grass g)
  (...(Grass-x g)(Grass-h g)))

;;Template Rules Used
;; Compound Data: 2 Fields

;; =================
;; Functions:

;; Grass -> Grass
;; start the world with ...
;; 
(define (main Grass)
  (big-bang Grass                      ; Grass
            (on-tick   grow)           ; Grass -> Grass
            (to-draw   render-grass)   ; Grass -> Image
            (on-key    handle-key)))          ; Grass KeyEvent -> Grass

;; Grass -> Grass
;; produce the next height of the grass
;;HELPER FUNCTION BELOW to check for growth
#;(define (valid-growth? g x h)
  (and (Grass? g)
       (= (Grass-x g) x)
       (>= (Grass-h g) (+ 1 h))
       (<= (Grass-h g) (+ 10 h))))
#;(valid-growth? (grow (make-Grass 10 10)) 10 10)

#;(define (grow g) ..g.) ;; STUB

;; Function Template from ABOVE
(define (grow g)
  (make-Grass (Grass-x g)
              (+(Grass-h g) GROWTH-RATE)))

;; Grass -> Image
;; render the X -coord and image of grass 

#;(define (render-grass g) ... MTS) ;; STUB
;; Function Template from ABOVE
(define (render-grass g)
  (place-image (draw-grass g) (Grass-x g) CTR-Y MTS))

;;Helper function
;; Grass -> Img
;; Returns an image of grass with the correct height
(define (draw-grass g)
  (rectangle GRASS-WIDTH (Grass-h g) "solid" "green"))


;; Grass KeyEvent -> Grass
;; render a new grass image and advance 10px on x-coord after 'any key'
;; 
#;(define (handle-key g ke) ...MTS) ;; STUB
;; Function Template from ABOVE
(define (handle-key g ke)
  (make-Grass (+ (Grass-x g) NEXT-POS-X) 0))


```


### Compound P5
Design a world to represent grass that grows and is replanted

[growing-grass-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/growing-grass-starter.rkt "growing-grass-starter.rkt")  
[growing-grass-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/growing-grass-solution.rkt "growing-grass-solution.rkt")

```LISP

```

### Compound P6

Design a world where the mouse position is displayed at the mouse cursor.

[tracker-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/tracker-starter.rkt "tracker-starter.rkt")  
[tracker-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/tracker-solution.rkt "tracker-solution.rkt")
[
```LISP
(require 2htdp/image)
(require 2htdp/universe)

; 
; PROBLEM:
; 
; Design a world program that displays the current (x, y) position
; of the mouse at that current position. So as the mouse moves the 
; numbers in the (x, y) display changes and its position changes. 
; 



(require 2htdp/image)
(require 2htdp/universe)

;; Mouse Tracker Display

;; =================
;; Constants:
(define WIDTH 900)
(define HEIGHT 450)
(define MTS (rectangle WIDTH HEIGHT "solid" "black"))
(define DISPLAY (rectangle 175 50 "outline" "white"))


;; =================
;; Data definitions:
(define-struct mt (x y))
;; MouseState is (make-mt Number Number)
;; Interp as The coords of the cursor
;;               x as the x-axis
;;               y as the y axis
(define M1 (make-mt 10 10))
(define M2 (make-mt 90 100))

(define (fn-for-mt m) (... (mt-x m) (mt-y m)))
;;Template Rules Used
;; Compound Data - 2 fileds

;; =================
;; Functions:

;; MouseState -> MouseState
;; start the world with (main (...))
;; 
(define (main mt)
  (big-bang mt                              ; MouseState
            (on-tick   tock)                ; MouseState -> MouseState
            (to-draw   render)              ; MouseState -> Image
            (on-mouse  handle-mouse) ))     ; MouseState Integer Integer MouseEvent -> MouseState


;; MouseState -> MouseState
;; produce the state of x, y coord of the mouse
;;
;(check-expect (tock M1)(make- 
(define (tock mt) mt) ;; STUB

;; Function Template from ABOVE
#;(define (fn-for-mt m)
  (handle-mouse m ))

;; MouseState -> Image
;; render ... 
;; !!! &Tests
;; (define (render mt)MTS) ;; STUB

(define (render mt)
  (place-image (text-maker mt) (- (mt-x mt)10) (- (mt-y mt)20) MTS))

;; HelperFunction
;; MOuseState -> ImageText
(define (text-maker mt)
 (overlay/align "center" "center"
                (text
                 (string-append (string-append "X: " (number->string (mt-x mt))) (string-append"  Y: " (number->string (mt-y mt))))
                 25 "white")DISPLAY))
;; Function Template from ABOVE

   
;; Our Tock Helper
;; MouseState X Y MouseEvent -> MouseState
;; render ... 
;; !!! &Tests
(define (handle-mouse mt x y me)
  (if (string=? me "move")
      (make-mt x y)
      mt))
;; Function Template from ABOVE

```


### Compound P7
Design a world where a lambda rolls back and forth across the screen.

[rolling-lambda-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/rolling-lambda-starter.rkt "rolling-lambda-starter.rkt")  
[rolling-lambda-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/rolling-lambda-solution.rkt "rolling-lambda-solution.rkt")

```LISP

```

### Compound P8
Write down the evaluation steps for an expression that has compound data.

[compound-evaluation-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/compound-evaluation-starter.rkt "compound-evaluation-starter.rkt")  
[compound-evaluation-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/compound-evaluation-solution.rkt "compound-evaluation-solution.rkt")

```LISP

```


### Compound P9

Design an animation of throwing a water balloon

[water-balloon-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/water-balloon-starter.rkt "water-balloon-starter.rkt")  
[water-balloon-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/water-balloon-solution.rkt "water-balloon-solution.rkt")


```LISP

;; water-balloon-starter.rkt

; PROBLEM:
; 
; In this problem, we will design an animation of throwing a water balloon.  
; When the program starts the water balloon should appear on the left side 
; of the screen, half-way up.  Since the balloon was thrown, it should 
; fly across the screen, rotating in a clockwise fashion. Pressing the 
; space key should cause the program to start over with the water balloon
; back at the left side of the screen. 
; 
; NOTE: Please include your domain analysis at the top in a comment box. 
; 
; Use the following images to assist you with your domain analysis:
; 
; 
; 1)
; 2).
; .
; 3)
; .
; 4)
; 
; .
;     
; 
; Here is an image of the water balloon:
; (define WATER-BALLOON.)
; 
; 
; 
; NOTE: The rotate function wants an angle in degrees as its first 
; argument. By that it means Number[0, 360). As time goes by your balloon 
; may end up spinning more than once, for example, you may get to a point 
; where it has spun 362 degrees, which rotate won't accept. 
; 
; The solution to that is to use the modulo function as follows:
; 
; (rotate (modulo ... 360) (text "hello" 30 "black"))
; 
; where ... should be replaced by the number of degrees to rotate.
; 
; NOTE: It is possible to design this program with simple atomic data, 
; but we would like you to use compound data.






(require 2htdp/image)
(require 2htdp/universe)

;; My world program  (make this more specific)

;; =================
;; Constants:
(define WATER-BALLOON .)

(define WIDTH 500)
(define HEIGHT 200)
(define MTS (empty-scene WIDTH HEIGHT))

(define CTR-Y (/ HEIGHT 2))
(define SPEED 10)
(define SPIN 10)

(define FIREKEY " ")
;; =================
;; Data definitions:
(define-struct Balloon (x angle))

;; Balloon is (make-Balloon(Natural[0, WIDTH] and Natural [0, 360])
;; Interp. as x being its postion moving across the screen
;;            angle being the degrees it rotates as it moves across the screen.
(define B1 (make-Balloon 10 10))
(define B2 (make-Balloon 90 350))

(define (fn-for-Balloon b)
  (... (Balloon-x b)       ; Natural [0, WIDTH]
       (Balloon-angle b))) ; Natural [0,   360]

;;TEMPLATE RULES USED
;;   - Compound Data: 2 Fields
;;
;; =================
;; Functions:

;; Balloon -> Balloon
;; start the world with (main( make-Balloon 0 0))
;; 
(define (main b)
  (big-bang b                   ; Balloon
            (on-tick   next-balloon)     ; Balloon -> Balloon
            (to-draw   render-balloon)   ; Balloon -> Image
            (on-key    Fire-Key)))    ; Balloon KeyEvent -> Balloon

;; Balloon -> Balloon
;; produce the next x postion and angle rotation on balloon and stop at width of screen.
;;

(check-expect(next-balloon (make-Balloon  10 10)) (make-Balloon 20  20)) ; balloon advances 10px and rotates 10 degrees

;(define (next-balloon b) ... b) ;STUB

(define (next-balloon b)
  (make-Balloon (+ (Balloon-x b) SPEED)  
       (+ (Balloon-angle b) SPIN)))


;; Balloon -> Image
;; render balloon at X Pos and at Rotational Pos 
;;

(check-expect(render-balloon (make-Balloon 10 10))  (place-image  (rotate 10 WATER-BALLOON) 10 CTR-Y MTS))
(check-expect(render-balloon (make-Balloon 60 361)) (place-image  (rotate 1  WATER-BALLOON)  60 CTR-Y MTS))
;: (define (render-balloon b) MTS) ; STUB

;; TEMPLATE FROM BALLOON ABOVE

(define (render-balloon b)
  (place-image
   (rotate (modulo (Balloon-angle b) 360)
           WATER-BALLOON) (Balloon-x b) CTR-Y MTS))


;; Balloon KeyEvent-> Balloon
;; Fire off a Ballon at x 0 when SPACEBAR pressed
;;

(check-expect(Fire-Key (make-Balloon 50 55) FIREKEY)  (make-Balloon 0    0)) ; Spacebar pressed
(check-expect(Fire-Key (make-Balloon 70 245)     "a") (make-Balloon 70 245)) ; Other Key pressed

;; (define (Fire-Key b ke) (... b)) ;STUB


;; TEMPLATE FROM BALLOON ABOVE

(define (Fire-Key b ke)
  (if (key=? ke FIREKEY)
  (make-Balloon 0 0)
  b))

```

### Compound P10

Design a simple one-line text editor.

[simple-text-editor-starter.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/simple-text-editor-starter.rkt "simple-text-editor-starter.rkt")  
[simple-text-editor-solution.rkt](https://s3.amazonaws.com/edx-course-spdx-kiczales/HTC/simple-text-editor-solution.rkt "simple-text-editor-solution.rkt")
```LISP

```


